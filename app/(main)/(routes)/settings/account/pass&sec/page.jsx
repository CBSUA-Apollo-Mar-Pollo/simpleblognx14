import PasswordAndSecurityCard from "@/components/SettingsComponent/PasswordAndSecurityCard";
import React from "react";

export const metadata = {
  title: `Settings | Password & Security`,
  description: "All in one social media app",
};

const PasswordAndSecurityPage = () => {
  return <PasswordAndSecurityCard />;
};

export default PasswordAndSecurityPage;
